To run the tests we need a Dropbox token. 

This should be stored in a file called `dropbox_login.py` 
that lives in this directory. This file should look like: 

`
login = {
	'access_token' : 'xxxxxxxxxxxx'
}
`